# Production site URL (no trailing slash). Used for canonical URLs, Open Graph, and JSON-LD.
# On Vercel you can set this to your custom domain. If unset, VERCEL_URL is used when available.
NEXT_PUBLIC_SITE_URL=https://ultimate-global-web.vercel.app
